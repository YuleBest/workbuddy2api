# Auth & secrets
auths/
data/
# 多 key 存储：keys.json 内含明文调用 key，绝不能进版本库（示例结构见 README 的"调用密钥"）
/keys.json
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
# /server /acct /stats 是本机部署用的名字（systemd ExecStart 指向仓库根的 ./server）：
# 漏了任何一个都会让十几 MB 的二进制被 git add -A 直接提交进来。
/login
/credit
/wb2api
/signin_bin
/server
/acct
/stats
/login.exe
/credit.exe
/wb2api.exe
/signin_bin.exe

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/

# 运行时产物（API Key / 进程号 / 日志 / 工具草稿目录）
# 为什么显式列 .apikey：它不是 config.json 的别名，而是 login.sh 等工具单独落盘的密钥文件，
# 不写进这里会被 git add -A 直接提交（含明文 api_key）。
.apikey
.server.pid
wb2api.pid
*.log
draft_*_folder/

# 前端依赖（管理面板源码在 web/，构建产物 internal/admin/dist 要进版本库）
node_modules/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md / DEVELOPMENT.md 必须进版本库）
*.md
!README.md
!DEVELOPMENT.md
